# Railway: сервис с Dockerfile → Variables → New Variable → имя BOT_TOKEN, значение = токен @BotFather → Redeploy.
BOT_TOKEN=

# Альтернатива: путь к файлу с токеном одной строкой (если платформа монтирует секрет как файл).
# BOT_TOKEN_FILE=/run/secrets/bot_token
